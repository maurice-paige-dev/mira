server {
    listen       80;
    server_name  _;
    root /usr/share/nginx/html;

    location / {
        try_files /chatbot$uri /chatbot/index.html;
    }

    location /catalog/ {
        alias /usr/share/nginx/html/catalog/;
        try_files $uri $uri/ =404;
    }
}
